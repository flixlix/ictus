# Changesets

Versioning and changelogs for this repo are managed by [Changesets](https://changesets.dev).

```bash
pnpm changeset
```

That writes a Markdown file in this folder. Merging to `main` opens a Version Packages PR. Merging that PR publishes to npm and creates a GitHub release.
